# my-react-app

A minimal Vite + React scaffold.

Quick start (Windows):

```bash
npm install
npm run dev
```

Build for production:

```bash
npm run build
npm run preview
```

Notes:
- The entry is `src/main.jsx` and the main component is `src/App.jsx`.
- Edit files and Vite will hot-reload the dev server.
